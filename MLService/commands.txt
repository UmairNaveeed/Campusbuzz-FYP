pip install sentencepiece && pip install -r requirements.txt && python3 app.py cd MLService
rm -rf venv
python3 -m venv venv
source venv/bin/activate
pip install --upgrade pip
pip install -r requirements.txt
python3 app.py